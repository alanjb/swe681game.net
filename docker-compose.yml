version: '3'

services:
  api-container:
    container_name: api_container1
    build:
      context: './'
      dockerfile: './api/Dockerfile'
    ports:
      - "8080:8080"
    volumes:
      - ./api:/usr/src/api