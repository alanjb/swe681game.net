version: '3'

services:
  api:
    container_name: api_container
    build:
      context: './'
      dockerfile: './api/Dockerfile'
    ports:
      - "8080:8080"
    volumes:
      - ./api:/usr/src/api